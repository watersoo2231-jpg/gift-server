# 선물하기 서버 연동 가이드

> 이 가이드를 순서대로 따라하면 **카페24 주문 완료 → 카카오 알림톡 자동 발송 → 배송지 입력 → 주문 자동 업데이트** 전체 흐름이 완성됩니다.

---

## 전체 흐름

```
[고객] 선물하기 버튼 클릭 → 받는 분 정보 입력 → 결제 완료
    ↓
[카페24] 주문 완료 웹훅 발송 → [선물하기 서버] 수신
    ↓
[선물하기 서버] 카카오 알림톡 발송 (배송지 입력 링크 포함)
    ↓
[받는 분] 알림톡 수신 → 링크 클릭 → 배송지 입력
    ↓
[선물하기 서버] 카페24 API로 주문 배송지 자동 업데이트
    ↓
[쇼핑몰] 배송 처리
```

---

## STEP 1 — 서버 호스팅 준비

서버는 **외부에서 접근 가능한 URL**이 있어야 합니다.
아래 중 하나를 선택하세요.

| 방법 | 비용 | 난이도 |
|------|------|--------|
| **카페24 호스팅** | 월 1~3만원 | 쉬움 |
| **AWS Lightsail** | 월 $5 | 보통 |
| **Heroku** | 무료~월 $7 | 쉬움 |
| **Railway.app** | 무료~월 $5 | 쉬움 |

서버 URL 예시: `https://gift.yourdomain.com`

---

## STEP 2 — 서버 설치 및 실행

```bash
# 1. 파일 업로드 후 디렉토리 이동
cd gift_server

# 2. 환경변수 파일 생성
cp .env.example .env
nano .env   # 아래 STEP들에서 발급받은 값들을 채워넣기

# 3. 패키지 설치
pip install -r requirements.txt

# 4. 서버 실행
bash start.sh
```

---

## STEP 3 — 카페24 앱 등록 및 API 키 발급

카페24 API를 사용하려면 개발자 앱 등록이 필요합니다.

1. [카페24 개발자 어드민](https://developers.cafe24.com) 접속
2. **앱 관리 → 앱 등록** 클릭
3. 아래 정보 입력:

| 항목 | 입력값 |
|------|--------|
| 앱 이름 | 선물하기 자동화 |
| 앱 유형 | Private App |
| Redirect URI | `https://서버URL/oauth/callback` |
| 권한(Scope) | `mall.read_order`, `mall.write_order` |

4. 등록 완료 후 **클라이언트 ID**, **클라이언트 시크릿** 복사
5. `.env` 파일에 입력:
```
CAFE24_CLIENT_ID=발급받은_클라이언트_ID
CAFE24_CLIENT_SECRET=발급받은_클라이언트_시크릿
```

---

## STEP 4 — 카페24 웹훅 등록

1. 카페24 개발자 어드민 → **앱 관리 → 내 앱 선택**
2. **WebHook → 등록** 클릭
3. 아래 정보 입력:

| 항목 | 입력값 |
|------|--------|
| 이벤트 | `mall.read_order` (주문 완료) |
| WebHook URL | `https://서버URL/webhook/cafe24/order` |
| 시크릿 키 | 임의의 문자열 (예: `gift_secret_2024`) |

4. 시크릿 키를 `.env`에 입력:
```
CAFE24_WEBHOOK_SECRET=설정한_시크릿키
```

---

## STEP 5 — 쿨SMS 가입 및 알림톡 설정

카카오 알림톡 발송은 **쿨SMS** 서비스를 통해 처리합니다.

### 5-1. 쿨SMS 가입
1. [쿨SMS](https://coolsms.co.kr) 가입
2. **개발자 콘솔 → API Key 관리 → 새 API Key 생성**
3. API Key, API Secret 복사 → `.env`에 입력:
```
COOLSMS_API_KEY=발급받은_API_KEY
COOLSMS_API_SECRET=발급받은_API_SECRET
COOLSMS_SENDER=010XXXXXXXX   # 발신번호 (사전 등록 필요)
```

### 5-2. 카카오 채널 연동
1. 쿨SMS 콘솔 → **카카오 채널 관리 → 채널 추가**
2. 보스님의 카카오 비즈니스 채널 ID 입력 (예: `@채널명`)
3. 채널 ID를 `.env`에 입력:
```
KAKAO_CHANNEL_ID=@채널아이디
```

### 5-3. 알림톡 템플릿 등록
1. 쿨SMS 콘솔 → **알림톡 → 템플릿 관리 → 새 템플릿**
2. 아래 내용으로 템플릿 작성:

```
[선물 도착 알림]

#{받는분이름}님, 안녕하세요!
#{보내는분이름}님이 소중한 선물을 보내셨어요 🎁

💌 선물 메시지:
#{선물메시지}

아래 버튼을 눌러 배송지를 입력해 주세요.
배송지 입력 후 빠르게 발송해 드리겠습니다.

⚠️ #{만료일}까지 입력하지 않으면 자동 취소됩니다.
```

3. 버튼 추가:
   - 버튼명: `배송지 입력하기`
   - 버튼 유형: `웹 링크`
   - 링크: `#{배송지입력링크}` (변수로 설정)

4. 카카오 검수 신청 → 승인 후 템플릿 코드 복사
5. `.env`에 입력:
```
KAKAO_TEMPLATE_ID=승인된_템플릿_코드
```

---

## STEP 6 — gift_address.html 업로드

1. 카페24 관리자 → **디자인 → 파일업로더**
2. `gift_address.html` 파일 업로드
3. 업로드 경로 확인 후 `.env`에 입력:
```
GIFT_ADDRESS_URL=https://pathocrionwater.cafe24.com/web/upload/gift_address.html
```

---

## STEP 7 — 최종 확인

서버가 정상 실행 중이라면 아래 URL로 상태를 확인할 수 있습니다:

```
GET https://서버URL/health
→ {"status": "ok", "time": "2026-02-20T..."}
```

### 테스트 방법
1. 쇼핑몰에서 선물하기 버튼 클릭
2. 받는 분 정보 입력 후 결제 완료
3. 받는 분 휴대폰으로 알림톡 수신 확인
4. 링크 클릭 → 배송지 입력
5. 카페24 관리자 → 주문 상세에서 배송지 업데이트 확인

---

## 파일 목록

| 파일명 | 설명 |
|--------|------|
| `app.py` | Flask 웹훅 서버 메인 코드 |
| `requirements.txt` | Python 패키지 목록 |
| `.env.example` | 환경변수 설정 예시 |
| `start.sh` | 서버 실행 스크립트 |
| `연동가이드.md` | 이 가이드 문서 |

---

## 자주 묻는 질문

**Q. 알림톡 대신 SMS로만 발송해도 되나요?**  
A. 네. 알림톡 설정 없이 쿨SMS만 설정하면 LMS(장문 문자)로 자동 대체 발송됩니다.

**Q. 카페24 앱 등록 없이 웹훅을 받을 수 있나요?**  
A. 카페24 공식 웹훅은 앱 등록이 필요합니다. 대안으로 카페24 주문 완료 페이지(order_result.html)에 JavaScript로 서버에 직접 요청을 보내는 방식도 가능합니다.

**Q. 서버 없이 운영할 수 있나요?**  
A. Zapier, Make(Integromat) 같은 노코드 자동화 도구를 활용하면 서버 없이도 유사한 흐름을 구현할 수 있습니다.
